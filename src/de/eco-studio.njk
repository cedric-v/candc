---
layout: base.njk
title: Eco Studio - Möblierte Wohnung
description: 34 m² Studio auf dem Land in der Nähe von Freiburg. Mindestaufenthalt 5 Nächte. Moderne und ökologische Ausstattung.
locale: de
permalink: /de/eco-studio/
ogImage: assets/img/logo-cc.jpg
---

{% set t = translations[locale] or translations['de'] %}

<section class="page-section">
  <div class="section-header">
    <h1 class="hero-title">{{ t.studio.title }}</h1>
    <p class="text-xl text-muted">{{ t.studio.subtitle }}</p>
    <p class="text-lg text-light">{{ t.studio.description }}</p>
    <p class="text-base font-semibold text-primary">{{ t.studio.minStay }}</p>
  </div>

  <div class="card">
    <h2 class="section-title mb-8">Ausstattung</h2>
    
    <div class="space-y-xl">
      <div>
        <h3 class="text-lg font-semibold text-primary mb-4">Schlafzimmer</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span class="text-xl">🛏️</span>
            <span class="text-muted">{{ t.studio.bedroom }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span class="text-xl">🛋️</span>
            <span class="text-muted">{{ t.studio.sofaBed }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Küche</h3>
        <p class="text-muted">{{ t.studio.kitchen }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Badezimmer</h3>
        <p class="text-muted">{{ t.studio.bathroom }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Wäsche</h3>
        <p class="text-muted">{{ t.studio.laundry }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Unterhaltung</h3>
        <p class="text-muted">{{ t.studio.entertainment }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Internet</h3>
        <p class="text-muted">{{ t.studio.internet }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-4">Außen</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span class="text-xl">🚗</span>
            <span class="text-muted">{{ t.studio.parking }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span class="text-xl">🌳</span>
            <span class="text-muted">{{ t.studio.terrace }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Ökologie</h3>
        <p class="text-muted">{{ t.studio.eco }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Familie</h3>
        <p class="text-muted">{{ t.studio.children }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Lage</h3>
        <p class="text-muted">{{ t.studio.location }}</p>
      </div>
    </div>

    <div class="section-header align-center mt-12">
      <div class="flex flex-wrap justify-center gap-lg">
        <a href="https://www.booking.com/hotel/ch/cedric-studio.fr.html" target="_blank" rel="noopener noreferrer" class="btn-primary">{{ t.bookOnBooking }}</a>
        <a href="https://fr.airbnb.ch/rooms/4116019" target="_blank" rel="noopener noreferrer" class="btn-primary">{{ t.bookOnAirbnb }}</a>
      </div>
      <div class="mt-6">
        <a href="https://share.google/4SQW4ioSzq3wkMAiA" target="_blank" rel="noopener noreferrer" class="btn-secondary">
          🗺️ {{ t.getDirections }}
        </a>
      </div>
    </div>
  </div>

  <!-- Business Section (visible octobre-février) -->
  {% set now = "now" | date("MM") | int %}
  {% if now >= 10 or now <= 2 %}
  <div class="card mt-12 border-primary">
    <h2 class="section-title mb-4">{{ t.businessTitle }}</h2>
    <p class="text-xl mb-8 text-muted">{{ t.businessSubtitle }}</p>
    <ul class="space-y-md">
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.wifi }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.quiet }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.office }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.location }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.monthly }}</span>
      </li>
    </ul>
  </div>
  {% endif %}

  <!-- Fotogalerie -->
  <section class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.gallery.title }}</h2>
      <p class="text-muted">{{ t.gallery.studioIntro }}</p>
    </div>
    <div class="gallery-grid">
      {% set galleryImages = [
        'assets/img/gallery/studio/studio-01.jpg',
        'assets/img/gallery/studio/studio-02.jpg',
        'assets/img/gallery/studio/studio-03.jpg',
        'assets/img/gallery/studio/studio-04.jpg',
        'assets/img/gallery/studio/studio-05.jpg',
        'assets/img/gallery/studio/studio-06.jpg'
      ] %}

      {% for image in galleryImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ image }}">
        {% image image, "Foto Eco Studio", "gallery-image", "lazy" %}
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Bewertungen -->
  <section id="testimonials" class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.testimonials.title }}</h2>
      <p class="text-muted">{{ t.testimonials.intro }}</p>
    </div>
    <div class="gallery-grid" id="testimonials-grid">
      {% set testimonialImages = [
        'assets/img/testimonials/testimonial-01.jpg',
        'assets/img/testimonials/testimonial-02.jpg',
        'assets/img/testimonials/testimonial-04.jpg',
        'assets/img/testimonials/testimonial-07.jpg',
        'assets/img/testimonials/testimonial-13.jpg',
        'assets/img/testimonials/testimonial-14.jpg',
        'assets/img/testimonials/testimonial-15.jpg',
        'assets/img/testimonials/testimonial-16.jpg',
        'assets/img/testimonials/testimonial-17.jpg',
        'assets/img/testimonials/testimonial-18.jpg',
        'assets/img/testimonials/testimonial-19.jpg',
        'assets/img/testimonials/testimonial-20.jpg',
        'assets/img/testimonials/testimonial-21.jpg',
        'assets/img/testimonials/testimonial-22.jpg',
        'assets/img/testimonials/testimonial-23.jpg',
        'assets/img/testimonials/testimonial-24.jpg',
        'assets/img/testimonials/testimonial-25.jpg'
      ] %}
      
      {% for image in testimonialImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ image }}">
        <img src="/{{ image }}" alt="Gästebewertung" class="gallery-image" loading="lazy">
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Lightbox -->
  <div id="eco-lightbox" class="lightbox">
    <button id="lightbox-close" class="lightbox-close" aria-label="Schließen">×</button>
    <button id="lightbox-prev" class="lightbox-prev" aria-label="Vorherige">‹</button>
    <button id="lightbox-next" class="lightbox-next" aria-label="Nächste">›</button>
    <div class="lightbox-container">
      <img id="lightbox-image" src="" alt="Foto">
    </div>
  </div>

  <script>
    (function() {
      const lightbox = document.getElementById('eco-lightbox');
      const lightboxImage = document.getElementById('lightbox-image');
      const closeBtn = document.getElementById('lightbox-close');
      const prevBtn = document.getElementById('lightbox-prev');
      const nextBtn = document.getElementById('lightbox-next');
      const triggers = document.querySelectorAll('.lightbox-trigger');
      
      let currentIndex = 0;
      let activeTriggers = [];
      
      function openLightbox(trigger) {
        const container = trigger.closest('.gallery-grid');
        activeTriggers = Array.from(container.querySelectorAll('.lightbox-trigger'));
        currentIndex = activeTriggers.indexOf(trigger);
        updateLightbox();
        lightbox.classList.add('flex');
        document.body.style.overflow = 'hidden';
      }
      
      function updateLightbox() {
        lightboxImage.src = activeTriggers[currentIndex].dataset.image;
      }
      
      function next() {
        currentIndex = (currentIndex + 1) % activeTriggers.length;
        updateLightbox();
      }
      
      function prev() {
        currentIndex = (currentIndex - 1 + activeTriggers.length) % activeTriggers.length;
        updateLightbox();
      }
      
      triggers.forEach(t => t.addEventListener('click', () => openLightbox(t)));
      
      closeBtn.onclick = () => {
        lightbox.classList.remove('flex');
        document.body.style.overflow = '';
      };
      
      nextBtn.onclick = next;
      prevBtn.onclick = prev;
      
      lightbox.onclick = (e) => {
        if (e.target === lightbox || e.target.classList.contains('lightbox-container')) {
          closeBtn.onclick();
        }
      };
      
      document.onkeydown = (e) => {
        if (!lightbox.classList.contains('flex')) return;
        if (e.key === 'Escape') closeBtn.onclick();
        if (e.key === 'ArrowRight') next();
        if (e.key === 'ArrowLeft') prev();
      };
    })();
  </script>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LodgingBusiness",
  "name": "C & C Eco Studio La Sonnaz",
  "description": "34m² möbliertes Studio mit Feldblick, nahe Freiburg",
  "url": "https://candc.ch/de/eco-studio/",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Impasse de la Pommeraie 5",
    "addressLocality": "Formangueires",
    "postalCode": "1782",
    "addressCountry": "CH"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "46.695",
    "longitude": "7.177"
  },
  "telephone": "+41-XX-XXX-XX-XX",
  "priceRange": "€€",
  "amenityFeature": [
    {
      "@type": "LocationFeatureSpecification",
      "name": "WiFi",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Parkplatz",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Terrasse",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Küche",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Waschmaschine",
      "value": true
    }
  ],
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.8",
    "reviewCount": "25"
  },
  "sameAs": [
    "https://www.booking.com/hotel/ch/cedric-studio.fr.html",
    "https://fr.airbnb.ch/rooms/4116019"
  ],
  "provider": {
    "@type": "Organization",
    "name": "C & C Eco Studio"
  }
}
</script>
