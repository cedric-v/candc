---
layout: base.njk
title: Parking - Aire de stationnement pour camping-car
description: Place de stationnement tout équipée pour van/camping-car/motorhome à La Sonnaz, Fribourg. Prise électrique, Wi-Fi, eau potable.
locale: fr
permalink: /fr/parking/
ogImage: assets/img/logo-cc.jpg
---

{% set t = translations[locale] or translations['fr'] %}

<section class="page-section">
  {% image "assets/img/fond-hero-parking.jpg", "Parking La Sonnaz", "page-hero-image", "eager" %}
  <div class="section-header">
    <h1 class="hero-title">{{ t.parking.title }}</h1>
    <p class="text-xl text-muted">{{ t.parking.subtitle }}</p>
    <p class="text-lg text-light">{{ t.parking.description }}</p>
  </div>

  <div class="card">
    <h2 class="section-title mb-8">Ce que nous fournissons</h2>
    <ul class="features-list">
      <li>
        <span class="text-xl">🅿️</span>
        <span class="text-muted">Place de stationnement</span>
      </li>
      <li>
        <span class="text-xl">⚡</span>
        <span class="text-muted">Prise électrique</span>
      </li>
      <li>
        <span class="text-xl">🗑️</span>
        <span class="text-muted">Gestion des déchets</span>
      </li>
      <li>
        <span class="text-xl">📶</span>
        <span class="text-muted">Wi-Fi rapide</span>
      </li>
      <li>
        <span class="text-xl">💧</span>
        <span class="text-muted">Robinet extérieur avec eau potable</span>
      </li>
      <li>
        <span class="text-xl">🌄</span>
        <span class="text-muted">Terrasse avec vue</span>
      </li>
    </ul>

    <div class="note-box mt-12">
      <p><strong class="text-primary">Note importante :</strong> {{ t.parking.note }}</p>
    </div>

    <div class="warning-box mt-12 mb-12">
      <p><strong class="text-xl">⚠️</strong> {{ t.parking.noChemicalToilet }}</p>
    </div>

    <div class="space-y-lg mb-12">
      <div class="service-box toilet">
        <h3 class="text-lg font-semibold text-primary mb-2">🚿 Accès toilette et douche</h3>
        <p class="text-muted">{{ t.parking.toilet }}</p>
      </div>

      <div class="service-box charging mt-8">
        <h3 class="text-lg font-semibold text-primary mb-2">🔌 Recharge voiture électrique</h3>
        <p class="text-muted">{{ t.parking.evCharging }}</p>
        <p class="text-sm text-light mt-2">Câble compatible Type 13 (220V, 10A) avec connecteur Type 2</p>
      </div>
    </div>

    <div class="mt-12 text-center" id="reservation">
      <a href="{{ t.googleMapsParkingLink }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
        🗺️ {{ t.getDirections }}
      </a>
    </div>

  </div>

  <!-- Galerie Photos -->
  <section class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.gallery.title }}</h2>
      <p class="text-muted">{{ t.gallery.parkingIntro }}</p>
    </div>
    <div class="gallery-grid">
      {% set galleryImages = [
        { src: 'assets/img/gallery/parking/parking-01.jpg', caption: "The parking space (9 meters long) au bord d'une impasse, à la campagne" },
        { src: 'assets/img/gallery/parking/parking-02.jpg', caption: "Terrace avec vue et Wifi" },
        { src: 'assets/img/gallery/parking/parking-03.jpg', caption: "Accès à un robinet d'eau potable" },
        { src: 'assets/img/gallery/parking/parking-04.jpg', caption: "Une prise électrique suisse (220V) est aussi à disposition" },
        { src: 'assets/img/gallery/parking/parking-05.jpg', caption: "Accès à un WC douche sur demande et quand nous sommes sur place" }
      ] %}

      {% for item in galleryImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ item.src }}" data-caption="{{ item.caption }}">
        {% image item.src, item.caption, "gallery-image", "lazy" %}
        <div class="gallery-caption-overlay">
          <span>{{ item.caption }}</span>
        </div>
      </div>
      {% endfor %}
    </div>
  </section>


  <!-- Témoignages (images) -->
  {% set pageTestimonials = testimonials.parking[locale] or testimonials.parking['fr'] or [] %}
  {% if pageTestimonials and pageTestimonials.length > 0 %}
  <section class="mt-3xl">
    <div class="section-header">
      <h2 class="section-title">Témoignages</h2>
      <p class="text-muted">Ce que nos hôtes disent</p>
    </div>
    <div class="gallery-grid" id="testimonials-grid">
      {% for testimonial in pageTestimonials %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ 'assets/img/testimonials/' + testimonial.image }}">
        {% if testimonial.image %}
          {% set imagePath = "assets/img/testimonials/" + testimonial.image %}
          {% set imageAlt = testimonial.alt or "Témoignage client" %}
          {% image imagePath, imageAlt, "gallery-image", "lazy" %}
        {% endif %}
      </div>
      {% endfor %}
    </div>
  </section>
  {% endif %}

  <!-- Moyens de paiement -->
  <section class="mt-3xl text-center">
    <h2 class="section-title mb-4">Moyens de paiement / Payment methods</h2>
    <p class="text-muted mb-8">
      Nous acceptons les cartes de crédit et le liquide (CHF ou EUR).
    </p>
    <div class="flex justify-center">
      <img src="/assets/img/payment-methods.jpg" alt="Moyens de paiement acceptés" class="rounded-lg shadow w-full max-w-2xl">
    </div>
  </section>

  <div class="mt-12 text-center pb-12">
    <h2 class="text-xl font-bold text-primary mb-6">Demande de réservation</h2>
    <div class="flex justify-center">
      <a href="/{{ locale }}/contact/" class="btn btn-primary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
          <polyline points="22,6 12,13 2,6"></polyline>
        </svg>
        <span class="ml-2">Demander une réservation</span>
      </a>
    </div>
  </div>
</section>

<!-- Lightbox -->
<div class="lightbox" id="gallery-lightbox">
  <button class="lightbox-close" id="lightbox-close" aria-label="Fermer">×</button>
  <button class="lightbox-prev" id="lightbox-prev" aria-label="Précédent">‹</button>
  <button class="lightbox-next" id="lightbox-next" aria-label="Suivant">›</button>
  <div class="lightbox-container">
    <img id="lightbox-image" src="" alt="Photo">
  </div>
</div>

<script>
  (function() {
    const lightbox = document.getElementById('gallery-lightbox');
    const lightboxImage = document.getElementById('lightbox-image');
    let lightboxCaption = document.getElementById('lightbox-caption');
    
    // Create caption element if it doesn't exist
    if (!lightboxCaption) {
      lightboxCaption = document.createElement('div');
      lightboxCaption.id = 'lightbox-caption';
      lightboxCaption.className = 'lightbox-caption';
      document.querySelector('.lightbox-container').appendChild(lightboxCaption);
    }

    const closeBtn = document.getElementById('lightbox-close');
    const prevBtn = document.getElementById('lightbox-prev');
    const nextBtn = document.getElementById('lightbox-next');
    const triggers = document.querySelectorAll('.lightbox-trigger');
    
    let currentIndex = 0;
    let activeTriggers = [];
    
    function openLightbox(trigger) {
      const container = trigger.closest('.gallery-grid, .testimonials-grid');
      activeTriggers = Array.from(container.querySelectorAll('.lightbox-trigger'));
      currentIndex = activeTriggers.indexOf(trigger);
      
      updateLightbox();
      lightbox.classList.remove('hidden');
      lightbox.classList.add('flex');
      document.body.style.overflow = 'hidden';
    }
    
    function updateLightbox() {
      const trigger = activeTriggers[currentIndex];
      lightboxImage.src = trigger.dataset.image;
      
      const caption = trigger.dataset.caption;
      if (caption) {
        lightboxCaption.textContent = caption;
        lightboxCaption.style.display = 'block';
      } else {
        lightboxCaption.style.display = 'none';
      }
    }
    
    function closeLightbox() {
      lightbox.classList.add('hidden');
      lightbox.classList.remove('flex');
      document.body.style.overflow = '';
    }
    
    function showNext() {
      currentIndex = (currentIndex + 1) % activeTriggers.length;
      updateLightbox();
    }
    
    function showPrev() {
      currentIndex = (currentIndex - 1 + activeTriggers.length) % activeTriggers.length;
      updateLightbox();
    }
    
    triggers.forEach((trigger) => {
      trigger.addEventListener('click', () => openLightbox(trigger));
    });
    
    closeBtn.addEventListener('click', closeLightbox);
    prevBtn.addEventListener('click', showPrev);
    nextBtn.addEventListener('click', showNext);
    
    lightbox.addEventListener('click', (e) => {
      if (e.target === lightbox || e.target.closest('.lightbox-container') === null) {
        if (e.target !== prevBtn && e.target !== nextBtn) {
          closeLightbox();
        }
      }
    });
    
    document.addEventListener('keydown', (e) => {
      if (!lightbox.classList.contains('hidden')) {
        if (e.key === 'Escape') closeLightbox();
        if (e.key === 'ArrowLeft') showPrev();
        if (e.key === 'ArrowRight') showNext();
      }
    });

    // Protection anti-spam
    const user = 'bonjour';
    const domain = 'candc';
    const tld = 'ch';
    const email = user + '@' + domain + '.' + tld;
    
    document.querySelectorAll('.email-obfuscated').forEach(el => {
      el.href = 'mailto:' + email;
      const label = el.querySelector('.email-label');
      if (!label) {
        el.textContent = email;
      }
    });
  })();
</script>
