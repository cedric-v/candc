---
layout: base.njk
title: Eco Studio - Location d'appartement meublé
description: Studio meublé de 34 m² en campagne proche de Fribourg. Séjour minimum 5 nuitées. Équipements modernes et écologiques.
locale: fr
permalink: /fr/eco-studio/
ogImage: assets/img/logo-cc.jpg
---

{% set t = translations[locale] or translations['fr'] %}



<section class="page-section">
  {% image "assets/img/fond-hero-studio.jpg", "Eco Studio La Sonnaz", "page-hero-image", "eager", "(max-width: 1200px) 100vw, 1200px" %}
  <div class="section-header">
    <h1 class="hero-title">{{ t.studio.title }}</h1>
    <p class="text-xl text-muted">{{ t.studio.subtitle }}</p>
    <p class="text-lg text-light">{{ t.studio.description }}</p>
    <p class="text-base font-semibold text-primary">{{ t.studio.minStay }}</p>
    <div class="gallery-grid mt-4" style="grid-template-columns: repeat(3, 1fr);">
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-1.jpg" data-caption="Wifi haut débit">
        {% image "assets/img/gallery/studio/gallerie-studio-1.jpg", "Aperçu studio", "gallery-image", "eager", "(max-width: 768px) 33vw, 400px" %}
      </div>
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-2.jpg" data-caption="Canapé-lit et lit double">
        {% image "assets/img/gallery/studio/gallerie-studio-2.jpg", "Aperçu studio", "gallery-image", "eager", "(max-width: 768px) 33vw, 400px" %}
      </div>
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-3.jpg" data-caption="WC douche et colonne de lavage-séchage">
        {% image "assets/img/gallery/studio/gallerie-studio-3.jpg", "Aperçu studio", "gallery-image", "eager", "(max-width: 768px) 33vw, 400px" %}
      </div>
    </div>
  </div>

  <div class="card">
    <h2 class="section-title mb-8">Équipements</h2>

    <div class="space-y-xl">
      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-4">Chambre</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span>🛏️</span>
            <span class="text-muted">{{ t.studio.bedroom }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span>🛋️</span>
            <span class="text-muted">{{ t.studio.sofaBed }}</span>
          </li>
        </ul>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Cuisine</h3>
        <p class="text-muted">{{ t.studio.kitchen }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Salle de bain</h3>
        <p class="text-muted">{{ t.studio.bathroom }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Linge</h3>
        <p class="text-muted">{{ t.studio.laundry }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Divertissement</h3>
        <p class="text-muted">{{ t.studio.entertainment }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Internet</h3>
        <p class="text-muted">{{ t.studio.internet }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-4">Extérieur</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span>🚗</span>
            <span class="text-muted">{{ t.studio.parking }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span>🌳</span>
            <span class="text-muted">{{ t.studio.terrace }}</span>
          </li>
        </ul>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Écologie</h3>
        <p class="text-muted">{{ t.studio.eco }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Famille</h3>
        <p class="text-muted">{{ t.studio.children }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Localisation</h3>
        <p class="text-muted">{{ t.studio.location }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-4">{{ t.studio.rules }}</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span>🚫🐾</span>
            <span class="text-muted">{{ t.studio.noPets }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span>🚬</span>
            <span class="text-muted">{{ t.studio.smokingOutdoor }}</span>
          </li>
        </ul>
      </div>

      <div class="service-box charging mt-8">
        <h3 class="text-lg font-semibold text-primary mb-2">{{ t.parking.evChargingTitle }}</h3>
        <p class="text-muted">{{ t.parking.evCharging }}</p>
        <p class="text-sm text-light mt-2">{{ t.parking.evCable }}</p>
      </div>
    </div>

    <div class="section-header align-center mt-12" id="reservation">
      <div class="flex flex-wrap justify-center gap-lg">
        <a href="https://www.booking.com/hotel/ch/cedric-studio.fr.html" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
          {{ t.bookOnBooking }}
        </a>
        <a href="https://www.airbnb.co.uk/rooms/4116019" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
          {{ t.bookOnAirbnb }}
        </a>
      </div>
      <div class="mt-6">
        <a href="{{ t.googleMapsStudioLink }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
          🗺️ {{ t.getDirections }}
        </a>
      </div>
      <div class="service-box policy mt-6 max-w-xl mx-auto">
        <p class="font-bold mb-2">{{ t.cancellationPolicy.title }}</p>
        <p class="text-sm mb-1">{{ t.cancellationPolicy.freeUntil }}</p>
        <p class="text-sm mb-2 text-primary font-semibold">{{ t.cancellationPolicy.fullRefund }}</p>
        <p class="text-xs text-light italic">{{ t.cancellationPolicy.note }}</p>
      </div>
    </div>
  </div>

  <!-- Business Section (visible octobre-février) -->
  {% set now = "now" | date("MM") | int %}
  {% if now >= 10 or now <= 2 %}
  <div class="card mt-12 border-primary">
    <h2 class="section-title mb-4">{{ t.businessTitle }}</h2>
    <p class="text-lg text-muted mb-8">{{ t.businessSubtitle }}</p>
    <ul class="space-y-md">
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.wifi }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.quiet }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.office }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.location | safe }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.monthly }}</span>
      </li>
    </ul>
  </div>
  {% endif %}

  <!-- Galerie Photos -->
  <section class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.gallery.title }}</h2>
      <p class="text-muted">{{ t.gallery.studioIntro }}</p>
    </div>
    <div class="gallery-grid">
      {% set galleryImages = [
        { src: 'assets/img/gallery/studio/gallerie-studio-bed.jpg', caption: t.gallery.doubleBed },
        { src: 'assets/img/gallery/studio/gallerie-studio-living-room.jpg', caption: t.gallery.independentEntry },
        { src: 'assets/img/gallery/studio/gallerie-studio-kitchen.jpg', caption: t.gallery.kitchen },
        { src: 'assets/img/gallery/studio/gallerie-studio-outside-terrace.jpg', caption: t.gallery.outsideTerrace },
        { src: 'assets/img/gallery/studio/gallerie-studio-outside-parking.jpg', caption: t.gallery.parkingSpace },
        { src: 'assets/img/gallery/studio/gallerie-studio-1.jpg', caption: t.gallery.highSpeedWifi },
        { src: 'assets/img/gallery/studio/gallerie-studio-2.jpg', caption: t.gallery.independentEntry },
        { src: 'assets/img/gallery/studio/gallerie-studio-3.jpg', caption: t.gallery.bathroom }
      ] %}

      {% for item in galleryImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ item.src }}" data-caption="{{ item.caption }}">
        {% image item.src, item.caption, "gallery-image", "lazy", "(max-width: 768px) 50vw, 300px" %}
        <div class="gallery-caption-overlay">
          <span>{{ item.caption }}</span>
        </div>
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Témoignages -->
  <section class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.testimonials.title }}</h2>
      <p class="text-muted">{{ t.testimonials.intro }}</p>
    </div>
    <div class="gallery-grid" id="testimonials-grid">
      {% set localTestimonials = testimonials.studio[locale] or testimonials.studio['fr'] %}
      {% for testimonial in localTestimonials %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ testimonial.image }}" data-caption="{{ testimonial.author }}: {{ testimonial.caption }}">
        <img src="/{{ testimonial.image }}" alt="{{ testimonial.alt }}" class="gallery-image" loading="lazy">
        <div class="gallery-caption-overlay">
          <span>{{ testimonial.author }}</span>
        </div>
      </div>
      {% endfor %}


    </div>
  </section>

  <!-- Lightbox -->
  <div class="lightbox" id="testimonial-lightbox">
    <button class="lightbox-close" id="lightbox-close" aria-label="Fermer">×</button>
    <button class="lightbox-prev" id="lightbox-prev" aria-label="Précédent">‹</button>
    <button class="lightbox-next" id="lightbox-next" aria-label="Suivant">›</button>
    <div class="lightbox-container">
      <img id="lightbox-image" src="" alt="Témoignage">
    </div>
  </div>

  <script>
    (function() {
      const lightbox = document.getElementById('testimonial-lightbox');
      const lightboxImage = document.getElementById('lightbox-image');
      let lightboxCaption = document.getElementById('lightbox-caption');
      
      // Create caption element if it doesn't exist
      if (!lightboxCaption) {
        lightboxCaption = document.createElement('div');
        lightboxCaption.id = 'lightbox-caption';
        lightboxCaption.className = 'lightbox-caption';
        document.querySelector('.lightbox-container').appendChild(lightboxCaption);
      }

      const closeBtn = document.getElementById('lightbox-close');
      const prevBtn = document.getElementById('lightbox-prev');
      const nextBtn = document.getElementById('lightbox-next');
      const triggers = document.querySelectorAll('.lightbox-trigger');
      
      let currentIndex = 0;
      let activeTriggers = [];
      
      // Swipe support
      let touchStartX = 0;
      let touchEndX = 0;

      function openLightbox(trigger) {
        const container = trigger.closest('.gallery-grid, #testimonials-grid');
        activeTriggers = Array.from(container.querySelectorAll('.lightbox-trigger'));
        currentIndex = activeTriggers.indexOf(trigger);
        
        updateLightbox();
        lightbox.classList.remove('hidden');
        lightbox.classList.add('flex');
        document.body.style.overflow = 'hidden';
      }
      
      function updateLightbox() {
        const trigger = activeTriggers[currentIndex];
        lightboxImage.src = trigger.dataset.image;
        
        const caption = trigger.dataset.caption;
        if (caption) {
          lightboxCaption.textContent = caption;
          lightboxCaption.style.display = 'block';
        } else {
          lightboxCaption.style.display = 'none';
        }
      }
      
      function closeLightbox() {
        lightbox.classList.add('hidden');
        lightbox.classList.remove('flex');
        document.body.style.overflow = '';
      }
      
      function showNext() {
        if (activeTriggers.length <= 1) return;
        currentIndex = (currentIndex + 1) % activeTriggers.length;
        updateLightbox();
      }
      
      function showPrev() {
        if (activeTriggers.length <= 1) return;
        currentIndex = (currentIndex - 1 + activeTriggers.length) % activeTriggers.length;
        updateLightbox();
      }
      
      function handleGesture() {
        if (touchEndX < touchStartX - 50) showNext();
        if (touchEndX > touchStartX + 50) showPrev();
      }

      triggers.forEach((trigger) => {
        trigger.addEventListener('click', (e) => {
          e.preventDefault();
          openLightbox(trigger);
        });
      });
      
      closeBtn.addEventListener('click', closeLightbox);
      prevBtn.addEventListener('click', (e) => { e.stopPropagation(); showPrev(); });
      nextBtn.addEventListener('click', (e) => { e.stopPropagation(); showNext(); });
      
      lightbox.addEventListener('click', (e) => {
        if (e.target === lightbox || e.target.closest('.lightbox-container') === null) {
          if (e.target !== prevBtn && e.target !== nextBtn) {
            closeLightbox();
          }
        }
      });

      lightbox.addEventListener('touchstart', e => {
        touchStartX = e.changedTouches[0].screenX;
      }, {passive: true});

      lightbox.addEventListener('touchend', e => {
        touchEndX = e.changedTouches[0].screenX;
        handleGesture();
      }, {passive: true});
      
      document.addEventListener('keydown', (e) => {
        if (!lightbox.classList.contains('hidden')) {
          if (e.key === 'Escape') closeLightbox();
          if (e.key === 'ArrowLeft') showPrev();
          if (e.key === 'ArrowRight') showNext();
        }
      });
    })();
  </script>
  <div class="mt-12 text-center pb-12">
    <h2 class="text-xl font-bold text-primary mb-6">{{ t.bookNow }}</h2>
    <div class="flex justify-center">
      <a href="#reservation" class="btn btn-primary">
        {{ t.checkAvailability }}
      </a>
    </div>
  </div>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LodgingBusiness",
  "name": "C & C Eco Studio La Sonnaz",
  "description": "{{ t.studio.description }}",
  "url": "https://candc.ch/{{ locale }}/eco-studio/",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Impasse de la Pommeraie 5",
    "addressLocality": "Formangueires",
    "postalCode": "1782",
    "addressCountry": "CH"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "46.695",
    "longitude": "7.177"
  },
  "telephone": "+41-XX-XXX-XX-XX",
  "priceRange": "€€",
  "amenityFeature": [
    {
      "@type": "LocationFeatureSpecification",
      "name": "WiFi",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Parking",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Terrace",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Kitchen",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Laundry",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "EV Charging",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Cancellation Policy",
      "value": "Free up to 48h before arrival"
    }
  ],
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.8",
    "reviewCount": "25"
  },
  "sameAs": [
    "https://www.booking.com/hotel/ch/cedric-studio.fr.html",
    "https://fr.airbnb.ch/rooms/4116019"
  ],
  "review": [
    {% for testimonial in testimonials.studio[locale] or testimonials.studio['fr'] %}
    {
      "@type": "Review",
      "author": {
        "@type": "Person",
        "name": "{{ testimonial.author }}"
      },
      "reviewRating": {
        "@type": "Rating",
        "ratingValue": "{{ testimonial.rating }}",
        "bestRating": "5"
      },
      "reviewBody": "{{ testimonial.alt }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
</script>
