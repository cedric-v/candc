---
layout: base.njk
title: Eco Studio - Location d'appartement meublé
description: Studio meublé de 34 m² en campagne proche de Fribourg. Séjour minimum 5 nuitées. Équipements modernes et écologiques.
locale: fr
permalink: /fr/eco-studio/
ogImage: assets/img/logo-cc.jpg
---

{% set t = translations[locale] or translations['fr'] %}

<section class="max-w-4xl mx-auto px-6 md:px-12 py-16 space-y-8">
  <div class="text-center space-y-4">
    <h1 class="text-4xl md:text-5xl font-bold text-[#2D5016]">{{ t.studio.title }}</h1>
    <p class="text-xl text-[#1f1f1f]/80">{{ t.studio.subtitle }}</p>
    <p class="text-lg text-[#1f1f1f]/70">{{ t.studio.description }}</p>
    <p class="text-base text-[#1f1f1f]/60 font-semibold">{{ t.studio.minStay }}</p>
  </div>

  <div class="section-card p-8 bg-white">
    <h2 class="text-2xl font-bold text-[#2D5016] mb-6">Équipements</h2>
    
    <div class="space-y-6">
      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Chambre</h3>
        <ul class="space-y-2 text-[#1f1f1f]/80">
          <li class="flex items-start gap-2">
            <span class="text-xl">🛏️</span>
            <span>{{ t.studio.bedroom }}</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-xl">🛋️</span>
            <span>{{ t.studio.sofaBed }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Cuisine</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.kitchen }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Salle de bain</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.bathroom }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Linge</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.laundry }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Divertissement</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.entertainment }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Internet</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.internet }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Extérieur</h3>
        <ul class="space-y-2 text-[#1f1f1f]/80">
          <li class="flex items-start gap-2">
            <span class="text-xl">🚗</span>
            <span>{{ t.studio.parking }}</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-xl">🌳</span>
            <span>{{ t.studio.terrace }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Écologie</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.eco }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Famille</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.children }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-[#2D5016] mb-3">Localisation</h3>
        <p class="text-[#1f1f1f]/80">{{ t.studio.location }}</p>
      </div>
    </div>

    <div class="mt-8 flex flex-wrap gap-4 justify-center">
      <a href="https://www.booking.com" target="_blank" rel="noopener noreferrer" class="btn-primary bg-[#6B8E23] text-white hover:bg-[#5a7a1d]">
        {{ t.bookOnBooking }}
      </a>
      <a href="https://www.airbnb.com" target="_blank" rel="noopener noreferrer" class="btn-primary bg-[#6B8E23] text-white hover:bg-[#5a7a1d]">
        {{ t.bookOnAirbnb }}
      </a>
      <a href="https://share.google/4SQW4ioSzq3wkMAiA" target="_blank" rel="noopener noreferrer" class="btn-secondary bg-white text-[#6B8E23] border-2 border-[#6B8E23] hover:bg-[#6B8E23] hover:text-white">
        🗺️ {{ t.getDirections }}
      </a>
    </div>
  </div>

  <!-- Business Section (visible octobre-février) -->
  {% set now = "now" | date("MM") | int %}
  {% if now >= 10 or now <= 2 %}
  <div class="section-card p-8 bg-white">
    <h2 class="text-3xl font-bold mb-4 text-[#2D5016]">{{ t.businessTitle }}</h2>
    <p class="text-xl mb-6 text-[#1f1f1f]/80">{{ t.businessSubtitle }}</p>
    <ul class="space-y-3 mb-6 text-[#1f1f1f]/80">
      <li class="flex items-start gap-2">
        <span class="text-2xl text-[#2D5016]">✓</span>
        <span>{{ t.businessFeatures.wifi }}</span>
      </li>
      <li class="flex items-start gap-2">
        <span class="text-2xl text-[#2D5016]">✓</span>
        <span>{{ t.businessFeatures.quiet }}</span>
      </li>
      <li class="flex items-start gap-2">
        <span class="text-2xl text-[#2D5016]">✓</span>
        <span>{{ t.businessFeatures.office }}</span>
      </li>
      <li class="flex items-start gap-2">
        <span class="text-2xl text-[#2D5016]">✓</span>
        <span>{{ t.businessFeatures.location }}</span>
      </li>
      <li class="flex items-start gap-2">
        <span class="text-2xl text-[#2D5016]">✓</span>
        <span>{{ t.businessFeatures.monthly }}</span>
      </li>
    </ul>
  </div>
  {% endif %}

  <!-- Témoignages -->
  <section id="testimonials" class="mt-12">
    <div class="text-center mb-8">
      <h2 class="text-3xl font-bold text-[#2D5016] mb-4">{{ t.testimonials.title }}</h2>
      <p class="text-lg text-[#1f1f1f]/80">{{ t.testimonials.intro }}</p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8" id="testimonials-grid">
      {% set testimonialImages = [
        'assets/img/testimonials/testimonial-01.jpg',
        'assets/img/testimonials/testimonial-02.jpg',
        'assets/img/testimonials/testimonial-04.jpg',
        'assets/img/testimonials/testimonial-07.jpg',
        'assets/img/testimonials/testimonial-13.jpg',
        'assets/img/testimonials/testimonial-14.jpg',
        'assets/img/testimonials/testimonial-15.jpg',
        'assets/img/testimonials/testimonial-16.jpg',
        'assets/img/testimonials/testimonial-17.jpg',
        'assets/img/testimonials/testimonial-18.jpg',
        'assets/img/testimonials/testimonial-19.jpg',
        'assets/img/testimonials/testimonial-20.jpg',
        'assets/img/testimonials/testimonial-21.jpg',
        'assets/img/testimonials/testimonial-22.jpg',
        'assets/img/testimonials/testimonial-23.jpg',
        'assets/img/testimonials/testimonial-24.jpg',
        'assets/img/testimonials/testimonial-25.jpg'
      ] %}
      
      {% for image in testimonialImages %}
      <div class="section-card overflow-hidden p-0 bg-white shadow-md hover:shadow-lg transition-shadow cursor-pointer testimonial-item" data-image="/{{ image }}">
        {% image image, "Témoignage client", "w-full h-auto object-cover", "lazy", "", "", "" %}
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Lightbox -->
  <div id="testimonial-lightbox" class="fixed inset-0 bg-black/90 z-50 hidden items-center justify-center">
    <button id="lightbox-close" class="absolute top-4 right-4 text-white text-5xl font-light hover:text-gray-300 transition z-10 w-12 h-12 flex items-center justify-center rounded-full hover:bg-white/10" aria-label="Fermer">×</button>
    <button id="lightbox-prev" class="absolute left-4 text-white text-6xl font-light hover:text-gray-300 transition z-10 w-16 h-16 flex items-center justify-center rounded-full hover:bg-white/10" aria-label="Précédent">‹</button>
    <button id="lightbox-next" class="absolute right-4 text-white text-6xl font-light hover:text-gray-300 transition z-10 w-16 h-16 flex items-center justify-center rounded-full hover:bg-white/10" aria-label="Suivant">›</button>
    <div class="max-w-6xl max-h-[90vh] p-8">
      <img id="lightbox-image" src="" alt="Témoignage" class="max-w-full max-h-[90vh] object-contain rounded-lg shadow-2xl">
    </div>
  </div>

  <script>
    (function() {
      const lightbox = document.getElementById('testimonial-lightbox');
      const lightboxImage = document.getElementById('lightbox-image');
      const closeBtn = document.getElementById('lightbox-close');
      const prevBtn = document.getElementById('lightbox-prev');
      const nextBtn = document.getElementById('lightbox-next');
      const items = document.querySelectorAll('.testimonial-item');
      
      let currentIndex = 0;
      const images = Array.from(items).map(item => item.dataset.image);
      
      function openLightbox(index) {
        currentIndex = index;
        lightboxImage.src = images[currentIndex];
        lightbox.classList.remove('hidden');
        lightbox.classList.add('flex');
        document.body.style.overflow = 'hidden';
      }
      
      function closeLightbox() {
        lightbox.classList.add('hidden');
        lightbox.classList.remove('flex');
        document.body.style.overflow = '';
      }
      
      function showNext() {
        currentIndex = (currentIndex + 1) % images.length;
        lightboxImage.src = images[currentIndex];
      }
      
      function showPrev() {
        currentIndex = (currentIndex - 1 + images.length) % images.length;
        lightboxImage.src = images[currentIndex];
      }
      
      items.forEach((item, index) => {
        item.addEventListener('click', () => openLightbox(index));
      });
      
      closeBtn.addEventListener('click', closeLightbox);
      prevBtn.addEventListener('click', showPrev);
      nextBtn.addEventListener('click', showNext);
      
      lightbox.addEventListener('click', (e) => {
        if (e.target === lightbox) closeLightbox();
      });
      
      document.addEventListener('keydown', (e) => {
        if (!lightbox.classList.contains('hidden')) {
          if (e.key === 'Escape') closeLightbox();
          if (e.key === 'ArrowLeft') showPrev();
          if (e.key === 'ArrowRight') showNext();
        }
      });
    })();
  </script>
</section>
