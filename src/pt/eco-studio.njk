---
layout: base.njk
title: Eco Studio - Aluguel de apartamento mobiliado
description: Estúdio de 34 m² no campo perto de Friburgo. Estadia mínima 5 noites. Equipamento moderno e ecológico.
locale: pt
permalink: /pt/eco-studio/
ogImage: assets/img/logo-cc.jpg
---

{% set t = translations[locale] or translations['pt'] %}

<section class="page-section">
  {% image "assets/img/fond-hero-studio.jpg", "Eco Studio La Sonnaz", "page-hero-image", "eager" %}
  <div class="section-header">
    <h1 class="hero-title">{{ t.studio.title }}</h1>
    <p class="text-xl text-muted">{{ t.studio.subtitle }}</p>
    <p class="text-lg text-light">{{ t.studio.description }}</p>
    <p class="text-base font-semibold text-primary">{{ t.studio.minStay }}</p>
    <div class="gallery-grid mt-4" style="grid-template-columns: repeat(3, 1fr);">
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-1.jpg" data-caption="{{ t.gallery.highSpeedWifi }}">
        {% image "assets/img/gallery/studio/gallerie-studio-1.jpg", "Studio preview", "gallery-image", "eager" %}
      </div>
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-2.jpg" data-caption="{{ t.gallery.independentEntry }}">
        {% image "assets/img/gallery/studio/gallerie-studio-2.jpg", "Studio preview", "gallery-image", "eager" %}
      </div>
      <div class="gallery-item lightbox-trigger" data-image="/assets/img/gallery/studio/gallerie-studio-3.jpg" data-caption="{{ t.gallery.clearView }}">
        {% image "assets/img/gallery/studio/gallerie-studio-3.jpg", "Studio preview", "gallery-image", "eager" %}
      </div>
    </div>
  </div>

  <div class="card">
    <h2 class="section-title mb-8">Equipamento</h2>
    
    <div class="space-y-xl">
      <div>
        <h3 class="text-lg font-semibold text-primary mb-4">Quarto</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span class="text-xl">🛏️</span>
            <span class="text-muted">{{ t.studio.bedroom }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span class="text-xl">🛋️</span>
            <span class="text-muted">{{ t.studio.sofaBed }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Cozinha</h3>
        <p class="text-muted">{{ t.studio.kitchen }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Casa de banho</h3>
        <p class="text-muted">{{ t.studio.bathroom }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Lavandaria</h3>
        <p class="text-muted">{{ t.studio.laundry }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Entretenimento</h3>
        <p class="text-muted">{{ t.studio.entertainment }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Internet</h3>
        <p class="text-muted">{{ t.studio.internet }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-4">Exterior</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span class="text-xl">🚗</span>
            <span class="text-muted">{{ t.studio.parking }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span class="text-xl">🌳</span>
            <span class="text-muted">{{ t.studio.terrace }}</span>
          </li>
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Ecologia</h3>
        <p class="text-muted">{{ t.studio.eco }}</p>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-primary mb-2">Família</h3>
        <p class="text-muted">{{ t.studio.children }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-2">Localização</h3>
        <p class="text-muted">{{ t.studio.location }}</p>
      </div>

      <div class="equipment-section">
        <h3 class="text-lg font-semibold text-primary mb-4">{{ t.studio.rules }}</h3>
        <ul class="space-y-sm">
          <li class="flex items-start gap-sm">
            <span>🚫🐾</span>
            <span class="text-muted">{{ t.studio.noPets }}</span>
          </li>
          <li class="flex items-start gap-sm">
            <span>🚬</span>
            <span class="text-muted">{{ t.studio.smokingOutdoor }}</span>
          </li>
        </ul>
      </div>

      <div class="service-box charging mt-8">
        <h3 class="text-lg font-semibold text-primary mb-2">{{ t.parking.evChargingTitle }}</h3>
        <p class="text-muted">{{ t.parking.evCharging }}</p>
        <p class="text-sm text-light mt-2">{{ t.parking.evCable }}</p>
      </div>
    </div>

    <div class="section-header align-center mt-12">
      <div class="flex flex-wrap justify-center gap-lg">
        <a href="https://www.booking.com/hotel/ch/cedric-studio.fr.html" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">{{ t.bookOnBooking }}</a>
        <a href="https://fr.airbnb.ch/rooms/4116019" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">{{ t.bookOnAirbnb }}</a>
      </div>
      <div class="mt-6">
        <a href="{{ t.googleMapsStudioLink }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
          🗺️ {{ t.getDirections }}
        </a>
      </div>
      <div class="service-box policy mt-6 max-w-xl mx-auto">
        <p class="font-bold mb-2">{{ t.cancellationPolicy.title }}</p>
        <p class="text-sm mb-1">{{ t.cancellationPolicy.freeUntil }}</p>
        <p class="text-sm mb-2 text-primary font-semibold">{{ t.cancellationPolicy.fullRefund }}</p>
        <p class="text-xs text-light italic">{{ t.cancellationPolicy.note }}</p>
      </div>
    </div>
  </div>

  <!-- Business Section (visible octobre-février) -->
  {% set now = "now" | date("MM") | int %}
  {% if now >= 10 or now <= 2 %}
  <div class="card mt-12 border-primary">
    <h2 class="section-title mb-4">{{ t.businessTitle }}</h2>
    <p class="text-xl mb-8 text-muted">{{ t.businessSubtitle }}</p>
    <ul class="space-y-md">
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.wifi }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.quiet }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.office }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.location | safe }}</span>
      </li>
      <li class="flex items-start gap-sm">
        <span class="text-primary font-bold">✓</span>
        <span class="text-muted">{{ t.businessFeatures.monthly }}</span>
      </li>
    </ul>
  </div>
  {% endif %}

  <!-- Galeria de Fotos -->
  <section class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.gallery.title }}</h2>
      <p class="text-muted">{{ t.gallery.studioIntro }}</p>
    </div>
    <div class="gallery-grid">
      {% set galleryImages = [
        { src: 'assets/img/gallery/studio/gallerie-studio-bed.jpg', caption: t.gallery.doubleBed },
        { src: 'assets/img/gallery/studio/gallerie-studio-living-room.jpg', caption: t.gallery.livingArea },
        { src: 'assets/img/gallery/studio/gallerie-studio-kitchen.jpg', caption: t.gallery.kitchen },
        { src: 'assets/img/gallery/studio/gallerie-studio-toilet-tumbler.jpg', caption: t.gallery.bathroom },
        { src: 'assets/img/gallery/studio/gallerie-studio-outside-terrace.jpg', caption: t.gallery.outsideTerrace },
        { src: 'assets/img/gallery/studio/gallerie-studio-outside-parking.jpg', caption: t.gallery.parkingSpace },
        { src: 'assets/img/gallery/studio/gallerie-studio-1.jpg', caption: t.gallery.highSpeedWifi },
        { src: 'assets/img/gallery/studio/gallerie-studio-2.jpg', caption: t.gallery.independentEntry },
        { src: 'assets/img/gallery/studio/gallerie-studio-3.jpg', caption: t.gallery.bathroom }
      ] %}

      {% for item in galleryImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ item.src }}" data-caption="{{ item.caption }}">
        {% image item.src, "Photo Eco Studio", "gallery-image", "lazy" %}
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Depoimentos -->
  <section id="testimonials" class="mt-2xl">
    <div class="section-header">
      <h2 class="section-title">{{ t.testimonials.title }}</h2>
      <p class="text-muted">{{ t.testimonials.intro }}</p>
    </div>
    <div class="gallery-grid" id="testimonials-grid">
      {% set testimonialImages = [
        'assets/img/testimonials/testimonial-01.jpg',
        'assets/img/testimonials/testimonial-02.jpg',
        'assets/img/testimonials/testimonial-04.jpg',
        'assets/img/testimonials/testimonial-07.jpg',
        'assets/img/testimonials/testimonial-13.jpg',
        'assets/img/testimonials/testimonial-14.jpg',
        'assets/img/testimonials/testimonial-15.jpg',
        'assets/img/testimonials/testimonial-16.jpg',
        'assets/img/testimonials/testimonial-17.jpg',
        'assets/img/testimonials/testimonial-18.jpg',
        'assets/img/testimonials/testimonial-19.jpg',
        'assets/img/testimonials/testimonial-20.jpg',
        'assets/img/testimonials/testimonial-21.jpg',
        'assets/img/testimonials/testimonial-22.jpg',
        'assets/img/testimonials/testimonial-23.jpg',
        'assets/img/testimonials/testimonial-24.jpg',
        'assets/img/testimonials/testimonial-25.jpg'
      ] %}
      
      {% for image in testimonialImages %}
      <div class="gallery-item lightbox-trigger" data-image="/{{ image }}">
        <img src="/{{ image }}" alt="Depoimento de hóspede" class="gallery-image" loading="lazy">
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- Lightbox -->
  <div id="eco-lightbox" class="lightbox">
    <button id="lightbox-close" class="lightbox-close" aria-label="Fechar">×</button>
    <button id="lightbox-prev" class="lightbox-prev" aria-label="Anterior">‹</button>
    <button id="lightbox-next" class="lightbox-next" aria-label="Próximo">›</button>
    <div class="lightbox-container">
      <img id="lightbox-image" src="" alt="Photo">
    </div>
  </div>

  <script>
    (function() {
      const lightbox = document.getElementById('eco-lightbox');
      const lightboxImage = document.getElementById('lightbox-image');
      let lightboxCaption = document.getElementById('lightbox-caption');
      
      // Create caption element if it doesn't exist
      if (!lightboxCaption) {
        lightboxCaption = document.createElement('div');
        lightboxCaption.id = 'lightbox-caption';
        lightboxCaption.className = 'lightbox-caption';
        document.querySelector('.lightbox-container').appendChild(lightboxCaption);
      }

      const closeBtn = document.getElementById('lightbox-close');
      const prevBtn = document.getElementById('lightbox-prev');
      const nextBtn = document.getElementById('lightbox-next');
      const triggers = document.querySelectorAll('.lightbox-trigger');
      
      let currentIndex = 0;
      let activeTriggers = [];
      
      // Swipe support
      let touchStartX = 0;
      let touchEndX = 0;

      function openLightbox(trigger) {
        const container = trigger.closest('.gallery-grid, #testimonials-grid');
        activeTriggers = Array.from(container.querySelectorAll('.lightbox-trigger'));
        currentIndex = activeTriggers.indexOf(trigger);
        
        updateLightbox();
        lightbox.classList.remove('hidden');
        lightbox.classList.add('flex');
        document.body.style.overflow = 'hidden';
      }
      
      function updateLightbox() {
        const trigger = activeTriggers[currentIndex];
        lightboxImage.src = trigger.dataset.image;
        
        const caption = trigger.dataset.caption;
        if (caption) {
          lightboxCaption.textContent = caption;
          lightboxCaption.style.display = 'block';
        } else {
          lightboxCaption.style.display = 'none';
        }
      }
      
      function closeLightbox() {
        lightbox.classList.remove('flex');
        lightbox.classList.add('hidden');
        document.body.style.overflow = '';
      }
      
      function next() {
        if (activeTriggers.length <= 1) return;
        currentIndex = (currentIndex + 1) % activeTriggers.length;
        updateLightbox();
      }
      
      function prev() {
        if (activeTriggers.length <= 1) return;
        currentIndex = (currentIndex - 1 + activeTriggers.length) % activeTriggers.length;
        updateLightbox();
      }
      
      function handleGesture() {
        if (touchEndX < touchStartX - 50) next();
        if (touchEndX > touchStartX + 50) prev();
      }

      triggers.forEach((trigger) => {
        trigger.addEventListener('click', (e) => {
          e.preventDefault();
          openLightbox(trigger);
        });
      });
      
      closeBtn.onclick = closeLightbox;
      prevBtn.onclick = (e) => { e.stopPropagation(); prev(); };
      nextBtn.onclick = (e) => { e.stopPropagation(); next(); };
      
      lightbox.onclick = (e) => {
        if (e.target === lightbox || e.target.classList.contains('lightbox-container')) {
          closeLightbox();
        }
      };

      lightbox.addEventListener('touchstart', e => {
        touchStartX = e.changedTouches[0].screenX;
      }, {passive: true});

      lightbox.addEventListener('touchend', e => {
        touchEndX = e.changedTouches[0].screenX;
        handleGesture();
      }, {passive: true});
      
      document.addEventListener('keydown', (e) => {
        if (lightbox.classList.contains('flex')) {
          if (e.key === 'Escape') closeLightbox();
          if (e.key === 'ArrowLeft') prev();
          if (e.key === 'ArrowRight') next();
        }
      });
    })();
  </script>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LodgingBusiness",
  "name": "C & C Eco Studio La Sonnaz",
  "description": "{{ t.studio.description }}",
  "url": "https://candc.ch/{{ locale }}/eco-studio/",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Impasse de la Pommeraie 5",
    "addressLocality": "Formangueires",
    "postalCode": "1782",
    "addressCountry": "CH"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "46.695",
    "longitude": "7.177"
  },
  "telephone": "+41-XX-XXX-XX-XX",
  "priceRange": "€€",
  "amenityFeature": [
    {
      "@type": "LocationFeatureSpecification",
      "name": "WiFi",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Parking",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Terrace",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Kitchen",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Laundry",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "EV Charging",
      "value": true
    },
    {
      "@type": "LocationFeatureSpecification",
      "name": "Cancellation Policy",
      "value": "Free up to 48h before arrival"
    }
  ],
  "sameAs": [
    "https://www.booking.com/hotel/ch/cedric-studio.fr.html",
    "https://fr.airbnb.ch/rooms/4116019"
  ],
  "review": [
    {% for testimonial in testimonials.studio[locale] or testimonials.studio['pt'] %}
    {
      "@type": "Review",
      "author": {
        "@type": "Person",
        "name": "{{ testimonial.author }}"
      },
      "reviewRating": {
        "@type": "Rating",
        "ratingValue": "{{ testimonial.rating }}",
        "bestRating": "5"
      },
      "reviewBody": "{{ testimonial.alt }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
</script>
